// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// RoomMembership is an object representing the database table.
type RoomMembership struct {
	TenantID   string `boil:"tenant_id" json:"tenant_id" toml:"tenant_id" yaml:"tenant_id"`
	RoomNid    int64  `boil:"room_nid" json:"room_nid" toml:"room_nid" yaml:"room_nid"`
	UserID     string `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	Membership string `boil:"membership" json:"membership" toml:"membership" yaml:"membership"`
	EventNid   int64  `boil:"event_nid" json:"event_nid" toml:"event_nid" yaml:"event_nid"`
	Forgotten  bool   `boil:"forgotten" json:"forgotten" toml:"forgotten" yaml:"forgotten"`

	R *roomMembershipR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L roomMembershipL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RoomMembershipColumns = struct {
	TenantID   string
	RoomNid    string
	UserID     string
	Membership string
	EventNid   string
	Forgotten  string
}{
	TenantID:   "tenant_id",
	RoomNid:    "room_nid",
	UserID:     "user_id",
	Membership: "membership",
	EventNid:   "event_nid",
	Forgotten:  "forgotten",
}

var RoomMembershipTableColumns = struct {
	TenantID   string
	RoomNid    string
	UserID     string
	Membership string
	EventNid   string
	Forgotten  string
}{
	TenantID:   "room_memberships.tenant_id",
	RoomNid:    "room_memberships.room_nid",
	UserID:     "room_memberships.user_id",
	Membership: "room_memberships.membership",
	EventNid:   "room_memberships.event_nid",
	Forgotten:  "room_memberships.forgotten",
}

// Generated where

var RoomMembershipWhere = struct {
	TenantID   whereHelperstring
	RoomNid    whereHelperint64
	UserID     whereHelperstring
	Membership whereHelperstring
	EventNid   whereHelperint64
	Forgotten  whereHelperbool
}{
	TenantID:   whereHelperstring{field: "\"room_memberships\".\"tenant_id\""},
	RoomNid:    whereHelperint64{field: "\"room_memberships\".\"room_nid\""},
	UserID:     whereHelperstring{field: "\"room_memberships\".\"user_id\""},
	Membership: whereHelperstring{field: "\"room_memberships\".\"membership\""},
	EventNid:   whereHelperint64{field: "\"room_memberships\".\"event_nid\""},
	Forgotten:  whereHelperbool{field: "\"room_memberships\".\"forgotten\""},
}

// RoomMembershipRels is where relationship names are stored.
var RoomMembershipRels = struct {
	EventNidEvent string
	RoomNidRoom   string
	Tenant        string
}{
	EventNidEvent: "EventNidEvent",
	RoomNidRoom:   "RoomNidRoom",
	Tenant:        "Tenant",
}

// roomMembershipR is where relationships are stored.
type roomMembershipR struct {
	EventNidEvent *Event  `boil:"EventNidEvent" json:"EventNidEvent" toml:"EventNidEvent" yaml:"EventNidEvent"`
	RoomNidRoom   *Room   `boil:"RoomNidRoom" json:"RoomNidRoom" toml:"RoomNidRoom" yaml:"RoomNidRoom"`
	Tenant        *Tenant `boil:"Tenant" json:"Tenant" toml:"Tenant" yaml:"Tenant"`
}

// NewStruct creates a new relationship struct
func (*roomMembershipR) NewStruct() *roomMembershipR {
	return &roomMembershipR{}
}

func (o *RoomMembership) GetEventNidEvent() *Event {
	if o == nil {
		return nil
	}

	return o.R.GetEventNidEvent()
}

func (r *roomMembershipR) GetEventNidEvent() *Event {
	if r == nil {
		return nil
	}

	return r.EventNidEvent
}

func (o *RoomMembership) GetRoomNidRoom() *Room {
	if o == nil {
		return nil
	}

	return o.R.GetRoomNidRoom()
}

func (r *roomMembershipR) GetRoomNidRoom() *Room {
	if r == nil {
		return nil
	}

	return r.RoomNidRoom
}

func (o *RoomMembership) GetTenant() *Tenant {
	if o == nil {
		return nil
	}

	return o.R.GetTenant()
}

func (r *roomMembershipR) GetTenant() *Tenant {
	if r == nil {
		return nil
	}

	return r.Tenant
}

// roomMembershipL is where Load methods for each relationship are stored.
type roomMembershipL struct{}

var (
	roomMembershipAllColumns            = []string{"tenant_id", "room_nid", "user_id", "membership", "event_nid", "forgotten"}
	roomMembershipColumnsWithoutDefault = []string{"tenant_id", "room_nid", "user_id", "membership", "event_nid"}
	roomMembershipColumnsWithDefault    = []string{"forgotten"}
	roomMembershipPrimaryKeyColumns     = []string{"tenant_id", "room_nid", "user_id"}
	roomMembershipGeneratedColumns      = []string{}
)

type (
	// RoomMembershipSlice is an alias for a slice of pointers to RoomMembership.
	// This should almost always be used instead of []RoomMembership.
	RoomMembershipSlice []*RoomMembership
	// RoomMembershipHook is the signature for custom RoomMembership hook methods
	RoomMembershipHook func(context.Context, boil.ContextExecutor, *RoomMembership) error

	roomMembershipQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	roomMembershipType                 = reflect.TypeOf(&RoomMembership{})
	roomMembershipMapping              = queries.MakeStructMapping(roomMembershipType)
	roomMembershipPrimaryKeyMapping, _ = queries.BindMapping(roomMembershipType, roomMembershipMapping, roomMembershipPrimaryKeyColumns)
	roomMembershipInsertCacheMut       sync.RWMutex
	roomMembershipInsertCache          = make(map[string]insertCache)
	roomMembershipUpdateCacheMut       sync.RWMutex
	roomMembershipUpdateCache          = make(map[string]updateCache)
	roomMembershipUpsertCacheMut       sync.RWMutex
	roomMembershipUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var roomMembershipAfterSelectMu sync.Mutex
var roomMembershipAfterSelectHooks []RoomMembershipHook

var roomMembershipBeforeInsertMu sync.Mutex
var roomMembershipBeforeInsertHooks []RoomMembershipHook
var roomMembershipAfterInsertMu sync.Mutex
var roomMembershipAfterInsertHooks []RoomMembershipHook

var roomMembershipBeforeUpdateMu sync.Mutex
var roomMembershipBeforeUpdateHooks []RoomMembershipHook
var roomMembershipAfterUpdateMu sync.Mutex
var roomMembershipAfterUpdateHooks []RoomMembershipHook

var roomMembershipBeforeDeleteMu sync.Mutex
var roomMembershipBeforeDeleteHooks []RoomMembershipHook
var roomMembershipAfterDeleteMu sync.Mutex
var roomMembershipAfterDeleteHooks []RoomMembershipHook

var roomMembershipBeforeUpsertMu sync.Mutex
var roomMembershipBeforeUpsertHooks []RoomMembershipHook
var roomMembershipAfterUpsertMu sync.Mutex
var roomMembershipAfterUpsertHooks []RoomMembershipHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RoomMembership) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range roomMembershipAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RoomMembership) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range roomMembershipBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RoomMembership) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range roomMembershipAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RoomMembership) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range roomMembershipBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RoomMembership) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range roomMembershipAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RoomMembership) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range roomMembershipBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RoomMembership) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range roomMembershipAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RoomMembership) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range roomMembershipBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RoomMembership) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range roomMembershipAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRoomMembershipHook registers your hook function for all future operations.
func AddRoomMembershipHook(hookPoint boil.HookPoint, roomMembershipHook RoomMembershipHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		roomMembershipAfterSelectMu.Lock()
		roomMembershipAfterSelectHooks = append(roomMembershipAfterSelectHooks, roomMembershipHook)
		roomMembershipAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		roomMembershipBeforeInsertMu.Lock()
		roomMembershipBeforeInsertHooks = append(roomMembershipBeforeInsertHooks, roomMembershipHook)
		roomMembershipBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		roomMembershipAfterInsertMu.Lock()
		roomMembershipAfterInsertHooks = append(roomMembershipAfterInsertHooks, roomMembershipHook)
		roomMembershipAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		roomMembershipBeforeUpdateMu.Lock()
		roomMembershipBeforeUpdateHooks = append(roomMembershipBeforeUpdateHooks, roomMembershipHook)
		roomMembershipBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		roomMembershipAfterUpdateMu.Lock()
		roomMembershipAfterUpdateHooks = append(roomMembershipAfterUpdateHooks, roomMembershipHook)
		roomMembershipAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		roomMembershipBeforeDeleteMu.Lock()
		roomMembershipBeforeDeleteHooks = append(roomMembershipBeforeDeleteHooks, roomMembershipHook)
		roomMembershipBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		roomMembershipAfterDeleteMu.Lock()
		roomMembershipAfterDeleteHooks = append(roomMembershipAfterDeleteHooks, roomMembershipHook)
		roomMembershipAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		roomMembershipBeforeUpsertMu.Lock()
		roomMembershipBeforeUpsertHooks = append(roomMembershipBeforeUpsertHooks, roomMembershipHook)
		roomMembershipBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		roomMembershipAfterUpsertMu.Lock()
		roomMembershipAfterUpsertHooks = append(roomMembershipAfterUpsertHooks, roomMembershipHook)
		roomMembershipAfterUpsertMu.Unlock()
	}
}

// One returns a single roomMembership record from the query.
func (q roomMembershipQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RoomMembership, error) {
	o := &RoomMembership{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for room_memberships")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RoomMembership records from the query.
func (q roomMembershipQuery) All(ctx context.Context, exec boil.ContextExecutor) (RoomMembershipSlice, error) {
	var o []*RoomMembership

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to RoomMembership slice")
	}

	if len(roomMembershipAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RoomMembership records in the query.
func (q roomMembershipQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count room_memberships rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q roomMembershipQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if room_memberships exists")
	}

	return count > 0, nil
}

// EventNidEvent pointed to by the foreign key.
func (o *RoomMembership) EventNidEvent(mods ...qm.QueryMod) eventQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"event_nid\" = ?", o.EventNid),
	}

	queryMods = append(queryMods, mods...)

	return Events(queryMods...)
}

// RoomNidRoom pointed to by the foreign key.
func (o *RoomMembership) RoomNidRoom(mods ...qm.QueryMod) roomQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"room_nid\" = ?", o.RoomNid),
	}

	queryMods = append(queryMods, mods...)

	return Rooms(queryMods...)
}

// Tenant pointed to by the foreign key.
func (o *RoomMembership) Tenant(mods ...qm.QueryMod) tenantQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TenantID),
	}

	queryMods = append(queryMods, mods...)

	return Tenants(queryMods...)
}

// LoadEventNidEvent allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (roomMembershipL) LoadEventNidEvent(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRoomMembership any, mods queries.Applicator) error {
	var slice []*RoomMembership
	var object *RoomMembership

	if singular {
		var ok bool
		object, ok = maybeRoomMembership.(*RoomMembership)
		if !ok {
			object = new(RoomMembership)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRoomMembership)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRoomMembership))
			}
		}
	} else {
		s, ok := maybeRoomMembership.(*[]*RoomMembership)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRoomMembership)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRoomMembership))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &roomMembershipR{}
		}
		args[object.EventNid] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &roomMembershipR{}
			}

			args[obj.EventNid] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`events`),
		qm.WhereIn(`events.event_nid in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Event")
	}

	var resultSlice []*Event
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Event")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for events")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for events")
	}

	if len(eventAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.EventNidEvent = foreign
		if foreign.R == nil {
			foreign.R = &eventR{}
		}
		foreign.R.EventNidRoomMemberships = append(foreign.R.EventNidRoomMemberships, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EventNid == foreign.EventNid {
				local.R.EventNidEvent = foreign
				if foreign.R == nil {
					foreign.R = &eventR{}
				}
				foreign.R.EventNidRoomMemberships = append(foreign.R.EventNidRoomMemberships, local)
				break
			}
		}
	}

	return nil
}

// LoadRoomNidRoom allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (roomMembershipL) LoadRoomNidRoom(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRoomMembership any, mods queries.Applicator) error {
	var slice []*RoomMembership
	var object *RoomMembership

	if singular {
		var ok bool
		object, ok = maybeRoomMembership.(*RoomMembership)
		if !ok {
			object = new(RoomMembership)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRoomMembership)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRoomMembership))
			}
		}
	} else {
		s, ok := maybeRoomMembership.(*[]*RoomMembership)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRoomMembership)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRoomMembership))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &roomMembershipR{}
		}
		args[object.RoomNid] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &roomMembershipR{}
			}

			args[obj.RoomNid] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`rooms`),
		qm.WhereIn(`rooms.room_nid in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Room")
	}

	var resultSlice []*Room
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Room")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for rooms")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for rooms")
	}

	if len(roomAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.RoomNidRoom = foreign
		if foreign.R == nil {
			foreign.R = &roomR{}
		}
		foreign.R.RoomNidRoomMemberships = append(foreign.R.RoomNidRoomMemberships, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.RoomNid == foreign.RoomNid {
				local.R.RoomNidRoom = foreign
				if foreign.R == nil {
					foreign.R = &roomR{}
				}
				foreign.R.RoomNidRoomMemberships = append(foreign.R.RoomNidRoomMemberships, local)
				break
			}
		}
	}

	return nil
}

// LoadTenant allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (roomMembershipL) LoadTenant(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRoomMembership any, mods queries.Applicator) error {
	var slice []*RoomMembership
	var object *RoomMembership

	if singular {
		var ok bool
		object, ok = maybeRoomMembership.(*RoomMembership)
		if !ok {
			object = new(RoomMembership)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRoomMembership)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRoomMembership))
			}
		}
	} else {
		s, ok := maybeRoomMembership.(*[]*RoomMembership)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRoomMembership)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRoomMembership))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &roomMembershipR{}
		}
		args[object.TenantID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &roomMembershipR{}
			}

			args[obj.TenantID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`tenants`),
		qm.WhereIn(`tenants.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Tenant")
	}

	var resultSlice []*Tenant
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Tenant")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for tenants")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for tenants")
	}

	if len(tenantAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Tenant = foreign
		if foreign.R == nil {
			foreign.R = &tenantR{}
		}
		foreign.R.RoomMemberships = append(foreign.R.RoomMemberships, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TenantID == foreign.ID {
				local.R.Tenant = foreign
				if foreign.R == nil {
					foreign.R = &tenantR{}
				}
				foreign.R.RoomMemberships = append(foreign.R.RoomMemberships, local)
				break
			}
		}
	}

	return nil
}

// SetEventNidEvent of the roomMembership to the related item.
// Sets o.R.EventNidEvent to related.
// Adds o to related.R.EventNidRoomMemberships.
func (o *RoomMembership) SetEventNidEvent(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Event) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"room_memberships\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"event_nid"}),
		strmangle.WhereClause("\"", "\"", 2, roomMembershipPrimaryKeyColumns),
	)
	values := []any{related.EventNid, o.TenantID, o.RoomNid, o.UserID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EventNid = related.EventNid
	if o.R == nil {
		o.R = &roomMembershipR{
			EventNidEvent: related,
		}
	} else {
		o.R.EventNidEvent = related
	}

	if related.R == nil {
		related.R = &eventR{
			EventNidRoomMemberships: RoomMembershipSlice{o},
		}
	} else {
		related.R.EventNidRoomMemberships = append(related.R.EventNidRoomMemberships, o)
	}

	return nil
}

// SetRoomNidRoom of the roomMembership to the related item.
// Sets o.R.RoomNidRoom to related.
// Adds o to related.R.RoomNidRoomMemberships.
func (o *RoomMembership) SetRoomNidRoom(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Room) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"room_memberships\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"room_nid"}),
		strmangle.WhereClause("\"", "\"", 2, roomMembershipPrimaryKeyColumns),
	)
	values := []any{related.RoomNid, o.TenantID, o.RoomNid, o.UserID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.RoomNid = related.RoomNid
	if o.R == nil {
		o.R = &roomMembershipR{
			RoomNidRoom: related,
		}
	} else {
		o.R.RoomNidRoom = related
	}

	if related.R == nil {
		related.R = &roomR{
			RoomNidRoomMemberships: RoomMembershipSlice{o},
		}
	} else {
		related.R.RoomNidRoomMemberships = append(related.R.RoomNidRoomMemberships, o)
	}

	return nil
}

// SetTenant of the roomMembership to the related item.
// Sets o.R.Tenant to related.
// Adds o to related.R.RoomMemberships.
func (o *RoomMembership) SetTenant(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Tenant) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"room_memberships\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"tenant_id"}),
		strmangle.WhereClause("\"", "\"", 2, roomMembershipPrimaryKeyColumns),
	)
	values := []any{related.ID, o.TenantID, o.RoomNid, o.UserID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TenantID = related.ID
	if o.R == nil {
		o.R = &roomMembershipR{
			Tenant: related,
		}
	} else {
		o.R.Tenant = related
	}

	if related.R == nil {
		related.R = &tenantR{
			RoomMemberships: RoomMembershipSlice{o},
		}
	} else {
		related.R.RoomMemberships = append(related.R.RoomMemberships, o)
	}

	return nil
}

// RoomMemberships retrieves all the records using an executor.
func RoomMemberships(mods ...qm.QueryMod) roomMembershipQuery {
	mods = append(mods, qm.From("\"room_memberships\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"room_memberships\".*"})
	}

	return roomMembershipQuery{q}
}

// FindRoomMembership retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRoomMembership(ctx context.Context, exec boil.ContextExecutor, tenantID string, roomNid int64, userID string, selectCols ...string) (*RoomMembership, error) {
	roomMembershipObj := &RoomMembership{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"room_memberships\" where \"tenant_id\"=$1 AND \"room_nid\"=$2 AND \"user_id\"=$3", sel,
	)

	q := queries.Raw(query, tenantID, roomNid, userID)

	err := q.Bind(ctx, exec, roomMembershipObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from room_memberships")
	}

	if err = roomMembershipObj.doAfterSelectHooks(ctx, exec); err != nil {
		return roomMembershipObj, err
	}

	return roomMembershipObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RoomMembership) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no room_memberships provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(roomMembershipColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	roomMembershipInsertCacheMut.RLock()
	cache, cached := roomMembershipInsertCache[key]
	roomMembershipInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			roomMembershipAllColumns,
			roomMembershipColumnsWithDefault,
			roomMembershipColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(roomMembershipType, roomMembershipMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(roomMembershipType, roomMembershipMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"room_memberships\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"room_memberships\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into room_memberships")
	}

	if !cached {
		roomMembershipInsertCacheMut.Lock()
		roomMembershipInsertCache[key] = cache
		roomMembershipInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RoomMembership.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RoomMembership) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	roomMembershipUpdateCacheMut.RLock()
	cache, cached := roomMembershipUpdateCache[key]
	roomMembershipUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			roomMembershipAllColumns,
			roomMembershipPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update room_memberships, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"room_memberships\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, roomMembershipPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(roomMembershipType, roomMembershipMapping, append(wl, roomMembershipPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update room_memberships row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for room_memberships")
	}

	if !cached {
		roomMembershipUpdateCacheMut.Lock()
		roomMembershipUpdateCache[key] = cache
		roomMembershipUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q roomMembershipQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for room_memberships")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for room_memberships")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RoomMembershipSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), roomMembershipPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"room_memberships\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, roomMembershipPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in roomMembership slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all roomMembership")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RoomMembership) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no room_memberships provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(roomMembershipColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	roomMembershipUpsertCacheMut.RLock()
	cache, cached := roomMembershipUpsertCache[key]
	roomMembershipUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			roomMembershipAllColumns,
			roomMembershipColumnsWithDefault,
			roomMembershipColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			roomMembershipAllColumns,
			roomMembershipPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert room_memberships, could not build update column list")
		}

		ret := strmangle.SetComplement(roomMembershipAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(roomMembershipPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert room_memberships, could not build conflict column list")
			}

			conflict = make([]string, len(roomMembershipPrimaryKeyColumns))
			copy(conflict, roomMembershipPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"room_memberships\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(roomMembershipType, roomMembershipMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(roomMembershipType, roomMembershipMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert room_memberships")
	}

	if !cached {
		roomMembershipUpsertCacheMut.Lock()
		roomMembershipUpsertCache[key] = cache
		roomMembershipUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RoomMembership record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RoomMembership) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no RoomMembership provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), roomMembershipPrimaryKeyMapping)
	sql := "DELETE FROM \"room_memberships\" WHERE \"tenant_id\"=$1 AND \"room_nid\"=$2 AND \"user_id\"=$3"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from room_memberships")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for room_memberships")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q roomMembershipQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no roomMembershipQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from room_memberships")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for room_memberships")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RoomMembershipSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(roomMembershipBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), roomMembershipPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"room_memberships\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, roomMembershipPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from roomMembership slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for room_memberships")
	}

	if len(roomMembershipAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RoomMembership) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRoomMembership(ctx, exec, o.TenantID, o.RoomNid, o.UserID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RoomMembershipSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RoomMembershipSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), roomMembershipPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"room_memberships\".* FROM \"room_memberships\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, roomMembershipPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in RoomMembershipSlice")
	}

	*o = slice

	return nil
}

// RoomMembershipExists checks if the RoomMembership row exists.
func RoomMembershipExists(ctx context.Context, exec boil.ContextExecutor, tenantID string, roomNid int64, userID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"room_memberships\" where \"tenant_id\"=$1 AND \"room_nid\"=$2 AND \"user_id\"=$3 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, tenantID, roomNid, userID)
	}
	row := exec.QueryRowContext(ctx, sql, tenantID, roomNid, userID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if room_memberships exists")
	}

	return exists, nil
}

// Exists checks if the RoomMembership row exists.
func (o *RoomMembership) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return RoomMembershipExists(ctx, exec, o.TenantID, o.RoomNid, o.UserID)
}
