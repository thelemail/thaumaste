// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// DeviceListChange is an object representing the database table.
type DeviceListChange struct {
	TenantID  string    `boil:"tenant_id" json:"tenant_id" toml:"tenant_id" yaml:"tenant_id"`
	UserID    string    `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	StreamID  int64     `boil:"stream_id" json:"stream_id" toml:"stream_id" yaml:"stream_id"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *deviceListChangeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L deviceListChangeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DeviceListChangeColumns = struct {
	TenantID  string
	UserID    string
	StreamID  string
	CreatedAt string
}{
	TenantID:  "tenant_id",
	UserID:    "user_id",
	StreamID:  "stream_id",
	CreatedAt: "created_at",
}

var DeviceListChangeTableColumns = struct {
	TenantID  string
	UserID    string
	StreamID  string
	CreatedAt string
}{
	TenantID:  "device_list_changes.tenant_id",
	UserID:    "device_list_changes.user_id",
	StreamID:  "device_list_changes.stream_id",
	CreatedAt: "device_list_changes.created_at",
}

// Generated where

var DeviceListChangeWhere = struct {
	TenantID  whereHelperstring
	UserID    whereHelperstring
	StreamID  whereHelperint64
	CreatedAt whereHelpertime_Time
}{
	TenantID:  whereHelperstring{field: "\"device_list_changes\".\"tenant_id\""},
	UserID:    whereHelperstring{field: "\"device_list_changes\".\"user_id\""},
	StreamID:  whereHelperint64{field: "\"device_list_changes\".\"stream_id\""},
	CreatedAt: whereHelpertime_Time{field: "\"device_list_changes\".\"created_at\""},
}

// DeviceListChangeRels is where relationship names are stored.
var DeviceListChangeRels = struct {
}{}

// deviceListChangeR is where relationships are stored.
type deviceListChangeR struct {
}

// NewStruct creates a new relationship struct
func (*deviceListChangeR) NewStruct() *deviceListChangeR {
	return &deviceListChangeR{}
}

// deviceListChangeL is where Load methods for each relationship are stored.
type deviceListChangeL struct{}

var (
	deviceListChangeAllColumns            = []string{"tenant_id", "user_id", "stream_id", "created_at"}
	deviceListChangeColumnsWithoutDefault = []string{"tenant_id", "user_id", "stream_id"}
	deviceListChangeColumnsWithDefault    = []string{"created_at"}
	deviceListChangePrimaryKeyColumns     = []string{"tenant_id", "user_id", "stream_id"}
	deviceListChangeGeneratedColumns      = []string{}
)

type (
	// DeviceListChangeSlice is an alias for a slice of pointers to DeviceListChange.
	// This should almost always be used instead of []DeviceListChange.
	DeviceListChangeSlice []*DeviceListChange
	// DeviceListChangeHook is the signature for custom DeviceListChange hook methods
	DeviceListChangeHook func(context.Context, boil.ContextExecutor, *DeviceListChange) error

	deviceListChangeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	deviceListChangeType                 = reflect.TypeOf(&DeviceListChange{})
	deviceListChangeMapping              = queries.MakeStructMapping(deviceListChangeType)
	deviceListChangePrimaryKeyMapping, _ = queries.BindMapping(deviceListChangeType, deviceListChangeMapping, deviceListChangePrimaryKeyColumns)
	deviceListChangeInsertCacheMut       sync.RWMutex
	deviceListChangeInsertCache          = make(map[string]insertCache)
	deviceListChangeUpdateCacheMut       sync.RWMutex
	deviceListChangeUpdateCache          = make(map[string]updateCache)
	deviceListChangeUpsertCacheMut       sync.RWMutex
	deviceListChangeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var deviceListChangeAfterSelectMu sync.Mutex
var deviceListChangeAfterSelectHooks []DeviceListChangeHook

var deviceListChangeBeforeInsertMu sync.Mutex
var deviceListChangeBeforeInsertHooks []DeviceListChangeHook
var deviceListChangeAfterInsertMu sync.Mutex
var deviceListChangeAfterInsertHooks []DeviceListChangeHook

var deviceListChangeBeforeUpdateMu sync.Mutex
var deviceListChangeBeforeUpdateHooks []DeviceListChangeHook
var deviceListChangeAfterUpdateMu sync.Mutex
var deviceListChangeAfterUpdateHooks []DeviceListChangeHook

var deviceListChangeBeforeDeleteMu sync.Mutex
var deviceListChangeBeforeDeleteHooks []DeviceListChangeHook
var deviceListChangeAfterDeleteMu sync.Mutex
var deviceListChangeAfterDeleteHooks []DeviceListChangeHook

var deviceListChangeBeforeUpsertMu sync.Mutex
var deviceListChangeBeforeUpsertHooks []DeviceListChangeHook
var deviceListChangeAfterUpsertMu sync.Mutex
var deviceListChangeAfterUpsertHooks []DeviceListChangeHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DeviceListChange) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range deviceListChangeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DeviceListChange) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range deviceListChangeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DeviceListChange) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range deviceListChangeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DeviceListChange) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range deviceListChangeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DeviceListChange) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range deviceListChangeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DeviceListChange) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range deviceListChangeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DeviceListChange) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range deviceListChangeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DeviceListChange) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range deviceListChangeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DeviceListChange) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range deviceListChangeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDeviceListChangeHook registers your hook function for all future operations.
func AddDeviceListChangeHook(hookPoint boil.HookPoint, deviceListChangeHook DeviceListChangeHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		deviceListChangeAfterSelectMu.Lock()
		deviceListChangeAfterSelectHooks = append(deviceListChangeAfterSelectHooks, deviceListChangeHook)
		deviceListChangeAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		deviceListChangeBeforeInsertMu.Lock()
		deviceListChangeBeforeInsertHooks = append(deviceListChangeBeforeInsertHooks, deviceListChangeHook)
		deviceListChangeBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		deviceListChangeAfterInsertMu.Lock()
		deviceListChangeAfterInsertHooks = append(deviceListChangeAfterInsertHooks, deviceListChangeHook)
		deviceListChangeAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		deviceListChangeBeforeUpdateMu.Lock()
		deviceListChangeBeforeUpdateHooks = append(deviceListChangeBeforeUpdateHooks, deviceListChangeHook)
		deviceListChangeBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		deviceListChangeAfterUpdateMu.Lock()
		deviceListChangeAfterUpdateHooks = append(deviceListChangeAfterUpdateHooks, deviceListChangeHook)
		deviceListChangeAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		deviceListChangeBeforeDeleteMu.Lock()
		deviceListChangeBeforeDeleteHooks = append(deviceListChangeBeforeDeleteHooks, deviceListChangeHook)
		deviceListChangeBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		deviceListChangeAfterDeleteMu.Lock()
		deviceListChangeAfterDeleteHooks = append(deviceListChangeAfterDeleteHooks, deviceListChangeHook)
		deviceListChangeAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		deviceListChangeBeforeUpsertMu.Lock()
		deviceListChangeBeforeUpsertHooks = append(deviceListChangeBeforeUpsertHooks, deviceListChangeHook)
		deviceListChangeBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		deviceListChangeAfterUpsertMu.Lock()
		deviceListChangeAfterUpsertHooks = append(deviceListChangeAfterUpsertHooks, deviceListChangeHook)
		deviceListChangeAfterUpsertMu.Unlock()
	}
}

// One returns a single deviceListChange record from the query.
func (q deviceListChangeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DeviceListChange, error) {
	o := &DeviceListChange{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for device_list_changes")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all DeviceListChange records from the query.
func (q deviceListChangeQuery) All(ctx context.Context, exec boil.ContextExecutor) (DeviceListChangeSlice, error) {
	var o []*DeviceListChange

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to DeviceListChange slice")
	}

	if len(deviceListChangeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all DeviceListChange records in the query.
func (q deviceListChangeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count device_list_changes rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q deviceListChangeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if device_list_changes exists")
	}

	return count > 0, nil
}

// DeviceListChanges retrieves all the records using an executor.
func DeviceListChanges(mods ...qm.QueryMod) deviceListChangeQuery {
	mods = append(mods, qm.From("\"device_list_changes\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"device_list_changes\".*"})
	}

	return deviceListChangeQuery{q}
}

// FindDeviceListChange retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDeviceListChange(ctx context.Context, exec boil.ContextExecutor, tenantID string, userID string, streamID int64, selectCols ...string) (*DeviceListChange, error) {
	deviceListChangeObj := &DeviceListChange{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"device_list_changes\" where \"tenant_id\"=$1 AND \"user_id\"=$2 AND \"stream_id\"=$3", sel,
	)

	q := queries.Raw(query, tenantID, userID, streamID)

	err := q.Bind(ctx, exec, deviceListChangeObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from device_list_changes")
	}

	if err = deviceListChangeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return deviceListChangeObj, err
	}

	return deviceListChangeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DeviceListChange) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no device_list_changes provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(deviceListChangeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	deviceListChangeInsertCacheMut.RLock()
	cache, cached := deviceListChangeInsertCache[key]
	deviceListChangeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			deviceListChangeAllColumns,
			deviceListChangeColumnsWithDefault,
			deviceListChangeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(deviceListChangeType, deviceListChangeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(deviceListChangeType, deviceListChangeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"device_list_changes\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"device_list_changes\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into device_list_changes")
	}

	if !cached {
		deviceListChangeInsertCacheMut.Lock()
		deviceListChangeInsertCache[key] = cache
		deviceListChangeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the DeviceListChange.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DeviceListChange) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	deviceListChangeUpdateCacheMut.RLock()
	cache, cached := deviceListChangeUpdateCache[key]
	deviceListChangeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			deviceListChangeAllColumns,
			deviceListChangePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update device_list_changes, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"device_list_changes\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, deviceListChangePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(deviceListChangeType, deviceListChangeMapping, append(wl, deviceListChangePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update device_list_changes row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for device_list_changes")
	}

	if !cached {
		deviceListChangeUpdateCacheMut.Lock()
		deviceListChangeUpdateCache[key] = cache
		deviceListChangeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q deviceListChangeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for device_list_changes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for device_list_changes")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DeviceListChangeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), deviceListChangePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"device_list_changes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, deviceListChangePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in deviceListChange slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all deviceListChange")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DeviceListChange) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no device_list_changes provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(deviceListChangeColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	deviceListChangeUpsertCacheMut.RLock()
	cache, cached := deviceListChangeUpsertCache[key]
	deviceListChangeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			deviceListChangeAllColumns,
			deviceListChangeColumnsWithDefault,
			deviceListChangeColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			deviceListChangeAllColumns,
			deviceListChangePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert device_list_changes, could not build update column list")
		}

		ret := strmangle.SetComplement(deviceListChangeAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(deviceListChangePrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert device_list_changes, could not build conflict column list")
			}

			conflict = make([]string, len(deviceListChangePrimaryKeyColumns))
			copy(conflict, deviceListChangePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"device_list_changes\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(deviceListChangeType, deviceListChangeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(deviceListChangeType, deviceListChangeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert device_list_changes")
	}

	if !cached {
		deviceListChangeUpsertCacheMut.Lock()
		deviceListChangeUpsertCache[key] = cache
		deviceListChangeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single DeviceListChange record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DeviceListChange) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no DeviceListChange provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), deviceListChangePrimaryKeyMapping)
	sql := "DELETE FROM \"device_list_changes\" WHERE \"tenant_id\"=$1 AND \"user_id\"=$2 AND \"stream_id\"=$3"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from device_list_changes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for device_list_changes")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q deviceListChangeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no deviceListChangeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from device_list_changes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for device_list_changes")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DeviceListChangeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(deviceListChangeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), deviceListChangePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"device_list_changes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, deviceListChangePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from deviceListChange slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for device_list_changes")
	}

	if len(deviceListChangeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DeviceListChange) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDeviceListChange(ctx, exec, o.TenantID, o.UserID, o.StreamID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DeviceListChangeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DeviceListChangeSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), deviceListChangePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"device_list_changes\".* FROM \"device_list_changes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, deviceListChangePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in DeviceListChangeSlice")
	}

	*o = slice

	return nil
}

// DeviceListChangeExists checks if the DeviceListChange row exists.
func DeviceListChangeExists(ctx context.Context, exec boil.ContextExecutor, tenantID string, userID string, streamID int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"device_list_changes\" where \"tenant_id\"=$1 AND \"user_id\"=$2 AND \"stream_id\"=$3 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, tenantID, userID, streamID)
	}
	row := exec.QueryRowContext(ctx, sql, tenantID, userID, streamID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if device_list_changes exists")
	}

	return exists, nil
}

// Exists checks if the DeviceListChange row exists.
func (o *DeviceListChange) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DeviceListChangeExists(ctx, exec, o.TenantID, o.UserID, o.StreamID)
}
