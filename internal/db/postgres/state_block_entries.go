// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// StateBlockEntry is an object representing the database table.
type StateBlockEntry struct {
	StateBlockNid    int64 `boil:"state_block_nid" json:"state_block_nid" toml:"state_block_nid" yaml:"state_block_nid"`
	EventTypeNid     int64 `boil:"event_type_nid" json:"event_type_nid" toml:"event_type_nid" yaml:"event_type_nid"`
	EventStateKeyNid int64 `boil:"event_state_key_nid" json:"event_state_key_nid" toml:"event_state_key_nid" yaml:"event_state_key_nid"`
	EventNid         int64 `boil:"event_nid" json:"event_nid" toml:"event_nid" yaml:"event_nid"`

	R *stateBlockEntryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L stateBlockEntryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var StateBlockEntryColumns = struct {
	StateBlockNid    string
	EventTypeNid     string
	EventStateKeyNid string
	EventNid         string
}{
	StateBlockNid:    "state_block_nid",
	EventTypeNid:     "event_type_nid",
	EventStateKeyNid: "event_state_key_nid",
	EventNid:         "event_nid",
}

var StateBlockEntryTableColumns = struct {
	StateBlockNid    string
	EventTypeNid     string
	EventStateKeyNid string
	EventNid         string
}{
	StateBlockNid:    "state_block_entries.state_block_nid",
	EventTypeNid:     "state_block_entries.event_type_nid",
	EventStateKeyNid: "state_block_entries.event_state_key_nid",
	EventNid:         "state_block_entries.event_nid",
}

// Generated where

var StateBlockEntryWhere = struct {
	StateBlockNid    whereHelperint64
	EventTypeNid     whereHelperint64
	EventStateKeyNid whereHelperint64
	EventNid         whereHelperint64
}{
	StateBlockNid:    whereHelperint64{field: "\"state_block_entries\".\"state_block_nid\""},
	EventTypeNid:     whereHelperint64{field: "\"state_block_entries\".\"event_type_nid\""},
	EventStateKeyNid: whereHelperint64{field: "\"state_block_entries\".\"event_state_key_nid\""},
	EventNid:         whereHelperint64{field: "\"state_block_entries\".\"event_nid\""},
}

// StateBlockEntryRels is where relationship names are stored.
var StateBlockEntryRels = struct {
	EventNidEvent                 string
	EventStateKeyNidEventStateKey string
	EventTypeNidEventType         string
	StateBlockNidStateBlock       string
}{
	EventNidEvent:                 "EventNidEvent",
	EventStateKeyNidEventStateKey: "EventStateKeyNidEventStateKey",
	EventTypeNidEventType:         "EventTypeNidEventType",
	StateBlockNidStateBlock:       "StateBlockNidStateBlock",
}

// stateBlockEntryR is where relationships are stored.
type stateBlockEntryR struct {
	EventNidEvent                 *Event         `boil:"EventNidEvent" json:"EventNidEvent" toml:"EventNidEvent" yaml:"EventNidEvent"`
	EventStateKeyNidEventStateKey *EventStateKey `boil:"EventStateKeyNidEventStateKey" json:"EventStateKeyNidEventStateKey" toml:"EventStateKeyNidEventStateKey" yaml:"EventStateKeyNidEventStateKey"`
	EventTypeNidEventType         *EventType     `boil:"EventTypeNidEventType" json:"EventTypeNidEventType" toml:"EventTypeNidEventType" yaml:"EventTypeNidEventType"`
	StateBlockNidStateBlock       *StateBlock    `boil:"StateBlockNidStateBlock" json:"StateBlockNidStateBlock" toml:"StateBlockNidStateBlock" yaml:"StateBlockNidStateBlock"`
}

// NewStruct creates a new relationship struct
func (*stateBlockEntryR) NewStruct() *stateBlockEntryR {
	return &stateBlockEntryR{}
}

func (o *StateBlockEntry) GetEventNidEvent() *Event {
	if o == nil {
		return nil
	}

	return o.R.GetEventNidEvent()
}

func (r *stateBlockEntryR) GetEventNidEvent() *Event {
	if r == nil {
		return nil
	}

	return r.EventNidEvent
}

func (o *StateBlockEntry) GetEventStateKeyNidEventStateKey() *EventStateKey {
	if o == nil {
		return nil
	}

	return o.R.GetEventStateKeyNidEventStateKey()
}

func (r *stateBlockEntryR) GetEventStateKeyNidEventStateKey() *EventStateKey {
	if r == nil {
		return nil
	}

	return r.EventStateKeyNidEventStateKey
}

func (o *StateBlockEntry) GetEventTypeNidEventType() *EventType {
	if o == nil {
		return nil
	}

	return o.R.GetEventTypeNidEventType()
}

func (r *stateBlockEntryR) GetEventTypeNidEventType() *EventType {
	if r == nil {
		return nil
	}

	return r.EventTypeNidEventType
}

func (o *StateBlockEntry) GetStateBlockNidStateBlock() *StateBlock {
	if o == nil {
		return nil
	}

	return o.R.GetStateBlockNidStateBlock()
}

func (r *stateBlockEntryR) GetStateBlockNidStateBlock() *StateBlock {
	if r == nil {
		return nil
	}

	return r.StateBlockNidStateBlock
}

// stateBlockEntryL is where Load methods for each relationship are stored.
type stateBlockEntryL struct{}

var (
	stateBlockEntryAllColumns            = []string{"state_block_nid", "event_type_nid", "event_state_key_nid", "event_nid"}
	stateBlockEntryColumnsWithoutDefault = []string{"state_block_nid", "event_type_nid", "event_state_key_nid", "event_nid"}
	stateBlockEntryColumnsWithDefault    = []string{}
	stateBlockEntryPrimaryKeyColumns     = []string{"state_block_nid", "event_type_nid", "event_state_key_nid"}
	stateBlockEntryGeneratedColumns      = []string{}
)

type (
	// StateBlockEntrySlice is an alias for a slice of pointers to StateBlockEntry.
	// This should almost always be used instead of []StateBlockEntry.
	StateBlockEntrySlice []*StateBlockEntry
	// StateBlockEntryHook is the signature for custom StateBlockEntry hook methods
	StateBlockEntryHook func(context.Context, boil.ContextExecutor, *StateBlockEntry) error

	stateBlockEntryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	stateBlockEntryType                 = reflect.TypeOf(&StateBlockEntry{})
	stateBlockEntryMapping              = queries.MakeStructMapping(stateBlockEntryType)
	stateBlockEntryPrimaryKeyMapping, _ = queries.BindMapping(stateBlockEntryType, stateBlockEntryMapping, stateBlockEntryPrimaryKeyColumns)
	stateBlockEntryInsertCacheMut       sync.RWMutex
	stateBlockEntryInsertCache          = make(map[string]insertCache)
	stateBlockEntryUpdateCacheMut       sync.RWMutex
	stateBlockEntryUpdateCache          = make(map[string]updateCache)
	stateBlockEntryUpsertCacheMut       sync.RWMutex
	stateBlockEntryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var stateBlockEntryAfterSelectMu sync.Mutex
var stateBlockEntryAfterSelectHooks []StateBlockEntryHook

var stateBlockEntryBeforeInsertMu sync.Mutex
var stateBlockEntryBeforeInsertHooks []StateBlockEntryHook
var stateBlockEntryAfterInsertMu sync.Mutex
var stateBlockEntryAfterInsertHooks []StateBlockEntryHook

var stateBlockEntryBeforeUpdateMu sync.Mutex
var stateBlockEntryBeforeUpdateHooks []StateBlockEntryHook
var stateBlockEntryAfterUpdateMu sync.Mutex
var stateBlockEntryAfterUpdateHooks []StateBlockEntryHook

var stateBlockEntryBeforeDeleteMu sync.Mutex
var stateBlockEntryBeforeDeleteHooks []StateBlockEntryHook
var stateBlockEntryAfterDeleteMu sync.Mutex
var stateBlockEntryAfterDeleteHooks []StateBlockEntryHook

var stateBlockEntryBeforeUpsertMu sync.Mutex
var stateBlockEntryBeforeUpsertHooks []StateBlockEntryHook
var stateBlockEntryAfterUpsertMu sync.Mutex
var stateBlockEntryAfterUpsertHooks []StateBlockEntryHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *StateBlockEntry) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range stateBlockEntryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *StateBlockEntry) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range stateBlockEntryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *StateBlockEntry) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range stateBlockEntryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *StateBlockEntry) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range stateBlockEntryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *StateBlockEntry) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range stateBlockEntryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *StateBlockEntry) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range stateBlockEntryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *StateBlockEntry) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range stateBlockEntryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *StateBlockEntry) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range stateBlockEntryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *StateBlockEntry) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range stateBlockEntryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddStateBlockEntryHook registers your hook function for all future operations.
func AddStateBlockEntryHook(hookPoint boil.HookPoint, stateBlockEntryHook StateBlockEntryHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		stateBlockEntryAfterSelectMu.Lock()
		stateBlockEntryAfterSelectHooks = append(stateBlockEntryAfterSelectHooks, stateBlockEntryHook)
		stateBlockEntryAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		stateBlockEntryBeforeInsertMu.Lock()
		stateBlockEntryBeforeInsertHooks = append(stateBlockEntryBeforeInsertHooks, stateBlockEntryHook)
		stateBlockEntryBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		stateBlockEntryAfterInsertMu.Lock()
		stateBlockEntryAfterInsertHooks = append(stateBlockEntryAfterInsertHooks, stateBlockEntryHook)
		stateBlockEntryAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		stateBlockEntryBeforeUpdateMu.Lock()
		stateBlockEntryBeforeUpdateHooks = append(stateBlockEntryBeforeUpdateHooks, stateBlockEntryHook)
		stateBlockEntryBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		stateBlockEntryAfterUpdateMu.Lock()
		stateBlockEntryAfterUpdateHooks = append(stateBlockEntryAfterUpdateHooks, stateBlockEntryHook)
		stateBlockEntryAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		stateBlockEntryBeforeDeleteMu.Lock()
		stateBlockEntryBeforeDeleteHooks = append(stateBlockEntryBeforeDeleteHooks, stateBlockEntryHook)
		stateBlockEntryBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		stateBlockEntryAfterDeleteMu.Lock()
		stateBlockEntryAfterDeleteHooks = append(stateBlockEntryAfterDeleteHooks, stateBlockEntryHook)
		stateBlockEntryAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		stateBlockEntryBeforeUpsertMu.Lock()
		stateBlockEntryBeforeUpsertHooks = append(stateBlockEntryBeforeUpsertHooks, stateBlockEntryHook)
		stateBlockEntryBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		stateBlockEntryAfterUpsertMu.Lock()
		stateBlockEntryAfterUpsertHooks = append(stateBlockEntryAfterUpsertHooks, stateBlockEntryHook)
		stateBlockEntryAfterUpsertMu.Unlock()
	}
}

// One returns a single stateBlockEntry record from the query.
func (q stateBlockEntryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*StateBlockEntry, error) {
	o := &StateBlockEntry{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for state_block_entries")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all StateBlockEntry records from the query.
func (q stateBlockEntryQuery) All(ctx context.Context, exec boil.ContextExecutor) (StateBlockEntrySlice, error) {
	var o []*StateBlockEntry

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to StateBlockEntry slice")
	}

	if len(stateBlockEntryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all StateBlockEntry records in the query.
func (q stateBlockEntryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count state_block_entries rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q stateBlockEntryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if state_block_entries exists")
	}

	return count > 0, nil
}

// EventNidEvent pointed to by the foreign key.
func (o *StateBlockEntry) EventNidEvent(mods ...qm.QueryMod) eventQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"event_nid\" = ?", o.EventNid),
	}

	queryMods = append(queryMods, mods...)

	return Events(queryMods...)
}

// EventStateKeyNidEventStateKey pointed to by the foreign key.
func (o *StateBlockEntry) EventStateKeyNidEventStateKey(mods ...qm.QueryMod) eventStateKeyQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"event_state_key_nid\" = ?", o.EventStateKeyNid),
	}

	queryMods = append(queryMods, mods...)

	return EventStateKeys(queryMods...)
}

// EventTypeNidEventType pointed to by the foreign key.
func (o *StateBlockEntry) EventTypeNidEventType(mods ...qm.QueryMod) eventTypeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"event_type_nid\" = ?", o.EventTypeNid),
	}

	queryMods = append(queryMods, mods...)

	return EventTypes(queryMods...)
}

// StateBlockNidStateBlock pointed to by the foreign key.
func (o *StateBlockEntry) StateBlockNidStateBlock(mods ...qm.QueryMod) stateBlockQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"state_block_nid\" = ?", o.StateBlockNid),
	}

	queryMods = append(queryMods, mods...)

	return StateBlocks(queryMods...)
}

// LoadEventNidEvent allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (stateBlockEntryL) LoadEventNidEvent(ctx context.Context, e boil.ContextExecutor, singular bool, maybeStateBlockEntry any, mods queries.Applicator) error {
	var slice []*StateBlockEntry
	var object *StateBlockEntry

	if singular {
		var ok bool
		object, ok = maybeStateBlockEntry.(*StateBlockEntry)
		if !ok {
			object = new(StateBlockEntry)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeStateBlockEntry)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeStateBlockEntry))
			}
		}
	} else {
		s, ok := maybeStateBlockEntry.(*[]*StateBlockEntry)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeStateBlockEntry)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeStateBlockEntry))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &stateBlockEntryR{}
		}
		args[object.EventNid] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &stateBlockEntryR{}
			}

			args[obj.EventNid] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`events`),
		qm.WhereIn(`events.event_nid in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Event")
	}

	var resultSlice []*Event
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Event")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for events")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for events")
	}

	if len(eventAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.EventNidEvent = foreign
		if foreign.R == nil {
			foreign.R = &eventR{}
		}
		foreign.R.EventNidStateBlockEntries = append(foreign.R.EventNidStateBlockEntries, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EventNid == foreign.EventNid {
				local.R.EventNidEvent = foreign
				if foreign.R == nil {
					foreign.R = &eventR{}
				}
				foreign.R.EventNidStateBlockEntries = append(foreign.R.EventNidStateBlockEntries, local)
				break
			}
		}
	}

	return nil
}

// LoadEventStateKeyNidEventStateKey allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (stateBlockEntryL) LoadEventStateKeyNidEventStateKey(ctx context.Context, e boil.ContextExecutor, singular bool, maybeStateBlockEntry any, mods queries.Applicator) error {
	var slice []*StateBlockEntry
	var object *StateBlockEntry

	if singular {
		var ok bool
		object, ok = maybeStateBlockEntry.(*StateBlockEntry)
		if !ok {
			object = new(StateBlockEntry)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeStateBlockEntry)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeStateBlockEntry))
			}
		}
	} else {
		s, ok := maybeStateBlockEntry.(*[]*StateBlockEntry)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeStateBlockEntry)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeStateBlockEntry))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &stateBlockEntryR{}
		}
		args[object.EventStateKeyNid] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &stateBlockEntryR{}
			}

			args[obj.EventStateKeyNid] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`event_state_keys`),
		qm.WhereIn(`event_state_keys.event_state_key_nid in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EventStateKey")
	}

	var resultSlice []*EventStateKey
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EventStateKey")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for event_state_keys")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for event_state_keys")
	}

	if len(eventStateKeyAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.EventStateKeyNidEventStateKey = foreign
		if foreign.R == nil {
			foreign.R = &eventStateKeyR{}
		}
		foreign.R.EventStateKeyNidStateBlockEntries = append(foreign.R.EventStateKeyNidStateBlockEntries, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EventStateKeyNid == foreign.EventStateKeyNid {
				local.R.EventStateKeyNidEventStateKey = foreign
				if foreign.R == nil {
					foreign.R = &eventStateKeyR{}
				}
				foreign.R.EventStateKeyNidStateBlockEntries = append(foreign.R.EventStateKeyNidStateBlockEntries, local)
				break
			}
		}
	}

	return nil
}

// LoadEventTypeNidEventType allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (stateBlockEntryL) LoadEventTypeNidEventType(ctx context.Context, e boil.ContextExecutor, singular bool, maybeStateBlockEntry any, mods queries.Applicator) error {
	var slice []*StateBlockEntry
	var object *StateBlockEntry

	if singular {
		var ok bool
		object, ok = maybeStateBlockEntry.(*StateBlockEntry)
		if !ok {
			object = new(StateBlockEntry)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeStateBlockEntry)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeStateBlockEntry))
			}
		}
	} else {
		s, ok := maybeStateBlockEntry.(*[]*StateBlockEntry)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeStateBlockEntry)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeStateBlockEntry))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &stateBlockEntryR{}
		}
		args[object.EventTypeNid] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &stateBlockEntryR{}
			}

			args[obj.EventTypeNid] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`event_types`),
		qm.WhereIn(`event_types.event_type_nid in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EventType")
	}

	var resultSlice []*EventType
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EventType")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for event_types")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for event_types")
	}

	if len(eventTypeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.EventTypeNidEventType = foreign
		if foreign.R == nil {
			foreign.R = &eventTypeR{}
		}
		foreign.R.EventTypeNidStateBlockEntries = append(foreign.R.EventTypeNidStateBlockEntries, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EventTypeNid == foreign.EventTypeNid {
				local.R.EventTypeNidEventType = foreign
				if foreign.R == nil {
					foreign.R = &eventTypeR{}
				}
				foreign.R.EventTypeNidStateBlockEntries = append(foreign.R.EventTypeNidStateBlockEntries, local)
				break
			}
		}
	}

	return nil
}

// LoadStateBlockNidStateBlock allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (stateBlockEntryL) LoadStateBlockNidStateBlock(ctx context.Context, e boil.ContextExecutor, singular bool, maybeStateBlockEntry any, mods queries.Applicator) error {
	var slice []*StateBlockEntry
	var object *StateBlockEntry

	if singular {
		var ok bool
		object, ok = maybeStateBlockEntry.(*StateBlockEntry)
		if !ok {
			object = new(StateBlockEntry)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeStateBlockEntry)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeStateBlockEntry))
			}
		}
	} else {
		s, ok := maybeStateBlockEntry.(*[]*StateBlockEntry)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeStateBlockEntry)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeStateBlockEntry))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &stateBlockEntryR{}
		}
		args[object.StateBlockNid] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &stateBlockEntryR{}
			}

			args[obj.StateBlockNid] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`state_blocks`),
		qm.WhereIn(`state_blocks.state_block_nid in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load StateBlock")
	}

	var resultSlice []*StateBlock
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice StateBlock")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for state_blocks")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for state_blocks")
	}

	if len(stateBlockAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.StateBlockNidStateBlock = foreign
		if foreign.R == nil {
			foreign.R = &stateBlockR{}
		}
		foreign.R.StateBlockNidStateBlockEntries = append(foreign.R.StateBlockNidStateBlockEntries, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.StateBlockNid == foreign.StateBlockNid {
				local.R.StateBlockNidStateBlock = foreign
				if foreign.R == nil {
					foreign.R = &stateBlockR{}
				}
				foreign.R.StateBlockNidStateBlockEntries = append(foreign.R.StateBlockNidStateBlockEntries, local)
				break
			}
		}
	}

	return nil
}

// SetEventNidEvent of the stateBlockEntry to the related item.
// Sets o.R.EventNidEvent to related.
// Adds o to related.R.EventNidStateBlockEntries.
func (o *StateBlockEntry) SetEventNidEvent(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Event) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"state_block_entries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"event_nid"}),
		strmangle.WhereClause("\"", "\"", 2, stateBlockEntryPrimaryKeyColumns),
	)
	values := []any{related.EventNid, o.StateBlockNid, o.EventTypeNid, o.EventStateKeyNid}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EventNid = related.EventNid
	if o.R == nil {
		o.R = &stateBlockEntryR{
			EventNidEvent: related,
		}
	} else {
		o.R.EventNidEvent = related
	}

	if related.R == nil {
		related.R = &eventR{
			EventNidStateBlockEntries: StateBlockEntrySlice{o},
		}
	} else {
		related.R.EventNidStateBlockEntries = append(related.R.EventNidStateBlockEntries, o)
	}

	return nil
}

// SetEventStateKeyNidEventStateKey of the stateBlockEntry to the related item.
// Sets o.R.EventStateKeyNidEventStateKey to related.
// Adds o to related.R.EventStateKeyNidStateBlockEntries.
func (o *StateBlockEntry) SetEventStateKeyNidEventStateKey(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EventStateKey) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"state_block_entries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"event_state_key_nid"}),
		strmangle.WhereClause("\"", "\"", 2, stateBlockEntryPrimaryKeyColumns),
	)
	values := []any{related.EventStateKeyNid, o.StateBlockNid, o.EventTypeNid, o.EventStateKeyNid}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EventStateKeyNid = related.EventStateKeyNid
	if o.R == nil {
		o.R = &stateBlockEntryR{
			EventStateKeyNidEventStateKey: related,
		}
	} else {
		o.R.EventStateKeyNidEventStateKey = related
	}

	if related.R == nil {
		related.R = &eventStateKeyR{
			EventStateKeyNidStateBlockEntries: StateBlockEntrySlice{o},
		}
	} else {
		related.R.EventStateKeyNidStateBlockEntries = append(related.R.EventStateKeyNidStateBlockEntries, o)
	}

	return nil
}

// SetEventTypeNidEventType of the stateBlockEntry to the related item.
// Sets o.R.EventTypeNidEventType to related.
// Adds o to related.R.EventTypeNidStateBlockEntries.
func (o *StateBlockEntry) SetEventTypeNidEventType(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EventType) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"state_block_entries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"event_type_nid"}),
		strmangle.WhereClause("\"", "\"", 2, stateBlockEntryPrimaryKeyColumns),
	)
	values := []any{related.EventTypeNid, o.StateBlockNid, o.EventTypeNid, o.EventStateKeyNid}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EventTypeNid = related.EventTypeNid
	if o.R == nil {
		o.R = &stateBlockEntryR{
			EventTypeNidEventType: related,
		}
	} else {
		o.R.EventTypeNidEventType = related
	}

	if related.R == nil {
		related.R = &eventTypeR{
			EventTypeNidStateBlockEntries: StateBlockEntrySlice{o},
		}
	} else {
		related.R.EventTypeNidStateBlockEntries = append(related.R.EventTypeNidStateBlockEntries, o)
	}

	return nil
}

// SetStateBlockNidStateBlock of the stateBlockEntry to the related item.
// Sets o.R.StateBlockNidStateBlock to related.
// Adds o to related.R.StateBlockNidStateBlockEntries.
func (o *StateBlockEntry) SetStateBlockNidStateBlock(ctx context.Context, exec boil.ContextExecutor, insert bool, related *StateBlock) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"state_block_entries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"state_block_nid"}),
		strmangle.WhereClause("\"", "\"", 2, stateBlockEntryPrimaryKeyColumns),
	)
	values := []any{related.StateBlockNid, o.StateBlockNid, o.EventTypeNid, o.EventStateKeyNid}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.StateBlockNid = related.StateBlockNid
	if o.R == nil {
		o.R = &stateBlockEntryR{
			StateBlockNidStateBlock: related,
		}
	} else {
		o.R.StateBlockNidStateBlock = related
	}

	if related.R == nil {
		related.R = &stateBlockR{
			StateBlockNidStateBlockEntries: StateBlockEntrySlice{o},
		}
	} else {
		related.R.StateBlockNidStateBlockEntries = append(related.R.StateBlockNidStateBlockEntries, o)
	}

	return nil
}

// StateBlockEntries retrieves all the records using an executor.
func StateBlockEntries(mods ...qm.QueryMod) stateBlockEntryQuery {
	mods = append(mods, qm.From("\"state_block_entries\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"state_block_entries\".*"})
	}

	return stateBlockEntryQuery{q}
}

// FindStateBlockEntry retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindStateBlockEntry(ctx context.Context, exec boil.ContextExecutor, stateBlockNid int64, eventTypeNid int64, eventStateKeyNid int64, selectCols ...string) (*StateBlockEntry, error) {
	stateBlockEntryObj := &StateBlockEntry{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"state_block_entries\" where \"state_block_nid\"=$1 AND \"event_type_nid\"=$2 AND \"event_state_key_nid\"=$3", sel,
	)

	q := queries.Raw(query, stateBlockNid, eventTypeNid, eventStateKeyNid)

	err := q.Bind(ctx, exec, stateBlockEntryObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from state_block_entries")
	}

	if err = stateBlockEntryObj.doAfterSelectHooks(ctx, exec); err != nil {
		return stateBlockEntryObj, err
	}

	return stateBlockEntryObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *StateBlockEntry) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no state_block_entries provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(stateBlockEntryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	stateBlockEntryInsertCacheMut.RLock()
	cache, cached := stateBlockEntryInsertCache[key]
	stateBlockEntryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			stateBlockEntryAllColumns,
			stateBlockEntryColumnsWithDefault,
			stateBlockEntryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(stateBlockEntryType, stateBlockEntryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(stateBlockEntryType, stateBlockEntryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"state_block_entries\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"state_block_entries\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into state_block_entries")
	}

	if !cached {
		stateBlockEntryInsertCacheMut.Lock()
		stateBlockEntryInsertCache[key] = cache
		stateBlockEntryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the StateBlockEntry.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *StateBlockEntry) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	stateBlockEntryUpdateCacheMut.RLock()
	cache, cached := stateBlockEntryUpdateCache[key]
	stateBlockEntryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			stateBlockEntryAllColumns,
			stateBlockEntryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update state_block_entries, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"state_block_entries\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, stateBlockEntryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(stateBlockEntryType, stateBlockEntryMapping, append(wl, stateBlockEntryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update state_block_entries row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for state_block_entries")
	}

	if !cached {
		stateBlockEntryUpdateCacheMut.Lock()
		stateBlockEntryUpdateCache[key] = cache
		stateBlockEntryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q stateBlockEntryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for state_block_entries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for state_block_entries")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o StateBlockEntrySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), stateBlockEntryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"state_block_entries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, stateBlockEntryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in stateBlockEntry slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all stateBlockEntry")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *StateBlockEntry) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no state_block_entries provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(stateBlockEntryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	stateBlockEntryUpsertCacheMut.RLock()
	cache, cached := stateBlockEntryUpsertCache[key]
	stateBlockEntryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			stateBlockEntryAllColumns,
			stateBlockEntryColumnsWithDefault,
			stateBlockEntryColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			stateBlockEntryAllColumns,
			stateBlockEntryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert state_block_entries, could not build update column list")
		}

		ret := strmangle.SetComplement(stateBlockEntryAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(stateBlockEntryPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert state_block_entries, could not build conflict column list")
			}

			conflict = make([]string, len(stateBlockEntryPrimaryKeyColumns))
			copy(conflict, stateBlockEntryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"state_block_entries\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(stateBlockEntryType, stateBlockEntryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(stateBlockEntryType, stateBlockEntryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert state_block_entries")
	}

	if !cached {
		stateBlockEntryUpsertCacheMut.Lock()
		stateBlockEntryUpsertCache[key] = cache
		stateBlockEntryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single StateBlockEntry record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *StateBlockEntry) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no StateBlockEntry provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), stateBlockEntryPrimaryKeyMapping)
	sql := "DELETE FROM \"state_block_entries\" WHERE \"state_block_nid\"=$1 AND \"event_type_nid\"=$2 AND \"event_state_key_nid\"=$3"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from state_block_entries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for state_block_entries")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q stateBlockEntryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no stateBlockEntryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from state_block_entries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for state_block_entries")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o StateBlockEntrySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(stateBlockEntryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), stateBlockEntryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"state_block_entries\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, stateBlockEntryPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from stateBlockEntry slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for state_block_entries")
	}

	if len(stateBlockEntryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *StateBlockEntry) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindStateBlockEntry(ctx, exec, o.StateBlockNid, o.EventTypeNid, o.EventStateKeyNid)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *StateBlockEntrySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := StateBlockEntrySlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), stateBlockEntryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"state_block_entries\".* FROM \"state_block_entries\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, stateBlockEntryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in StateBlockEntrySlice")
	}

	*o = slice

	return nil
}

// StateBlockEntryExists checks if the StateBlockEntry row exists.
func StateBlockEntryExists(ctx context.Context, exec boil.ContextExecutor, stateBlockNid int64, eventTypeNid int64, eventStateKeyNid int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"state_block_entries\" where \"state_block_nid\"=$1 AND \"event_type_nid\"=$2 AND \"event_state_key_nid\"=$3 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, stateBlockNid, eventTypeNid, eventStateKeyNid)
	}
	row := exec.QueryRowContext(ctx, sql, stateBlockNid, eventTypeNid, eventStateKeyNid)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if state_block_entries exists")
	}

	return exists, nil
}

// Exists checks if the StateBlockEntry row exists.
func (o *StateBlockEntry) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return StateBlockEntryExists(ctx, exec, o.StateBlockNid, o.EventTypeNid, o.EventStateKeyNid)
}
