// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// ToDeviceTransaction is an object representing the database table.
type ToDeviceTransaction struct {
	TenantID  string    `boil:"tenant_id" json:"tenant_id" toml:"tenant_id" yaml:"tenant_id"`
	UserID    string    `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	DeviceID  string    `boil:"device_id" json:"device_id" toml:"device_id" yaml:"device_id"`
	TXNID     string    `boil:"txn_id" json:"txn_id" toml:"txn_id" yaml:"txn_id"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *toDeviceTransactionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L toDeviceTransactionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ToDeviceTransactionColumns = struct {
	TenantID  string
	UserID    string
	DeviceID  string
	TXNID     string
	CreatedAt string
}{
	TenantID:  "tenant_id",
	UserID:    "user_id",
	DeviceID:  "device_id",
	TXNID:     "txn_id",
	CreatedAt: "created_at",
}

var ToDeviceTransactionTableColumns = struct {
	TenantID  string
	UserID    string
	DeviceID  string
	TXNID     string
	CreatedAt string
}{
	TenantID:  "to_device_transactions.tenant_id",
	UserID:    "to_device_transactions.user_id",
	DeviceID:  "to_device_transactions.device_id",
	TXNID:     "to_device_transactions.txn_id",
	CreatedAt: "to_device_transactions.created_at",
}

// Generated where

var ToDeviceTransactionWhere = struct {
	TenantID  whereHelperstring
	UserID    whereHelperstring
	DeviceID  whereHelperstring
	TXNID     whereHelperstring
	CreatedAt whereHelpertime_Time
}{
	TenantID:  whereHelperstring{field: "\"to_device_transactions\".\"tenant_id\""},
	UserID:    whereHelperstring{field: "\"to_device_transactions\".\"user_id\""},
	DeviceID:  whereHelperstring{field: "\"to_device_transactions\".\"device_id\""},
	TXNID:     whereHelperstring{field: "\"to_device_transactions\".\"txn_id\""},
	CreatedAt: whereHelpertime_Time{field: "\"to_device_transactions\".\"created_at\""},
}

// ToDeviceTransactionRels is where relationship names are stored.
var ToDeviceTransactionRels = struct {
}{}

// toDeviceTransactionR is where relationships are stored.
type toDeviceTransactionR struct {
}

// NewStruct creates a new relationship struct
func (*toDeviceTransactionR) NewStruct() *toDeviceTransactionR {
	return &toDeviceTransactionR{}
}

// toDeviceTransactionL is where Load methods for each relationship are stored.
type toDeviceTransactionL struct{}

var (
	toDeviceTransactionAllColumns            = []string{"tenant_id", "user_id", "device_id", "txn_id", "created_at"}
	toDeviceTransactionColumnsWithoutDefault = []string{"tenant_id", "user_id", "device_id", "txn_id"}
	toDeviceTransactionColumnsWithDefault    = []string{"created_at"}
	toDeviceTransactionPrimaryKeyColumns     = []string{"tenant_id", "user_id", "device_id", "txn_id"}
	toDeviceTransactionGeneratedColumns      = []string{}
)

type (
	// ToDeviceTransactionSlice is an alias for a slice of pointers to ToDeviceTransaction.
	// This should almost always be used instead of []ToDeviceTransaction.
	ToDeviceTransactionSlice []*ToDeviceTransaction
	// ToDeviceTransactionHook is the signature for custom ToDeviceTransaction hook methods
	ToDeviceTransactionHook func(context.Context, boil.ContextExecutor, *ToDeviceTransaction) error

	toDeviceTransactionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	toDeviceTransactionType                 = reflect.TypeOf(&ToDeviceTransaction{})
	toDeviceTransactionMapping              = queries.MakeStructMapping(toDeviceTransactionType)
	toDeviceTransactionPrimaryKeyMapping, _ = queries.BindMapping(toDeviceTransactionType, toDeviceTransactionMapping, toDeviceTransactionPrimaryKeyColumns)
	toDeviceTransactionInsertCacheMut       sync.RWMutex
	toDeviceTransactionInsertCache          = make(map[string]insertCache)
	toDeviceTransactionUpdateCacheMut       sync.RWMutex
	toDeviceTransactionUpdateCache          = make(map[string]updateCache)
	toDeviceTransactionUpsertCacheMut       sync.RWMutex
	toDeviceTransactionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var toDeviceTransactionAfterSelectMu sync.Mutex
var toDeviceTransactionAfterSelectHooks []ToDeviceTransactionHook

var toDeviceTransactionBeforeInsertMu sync.Mutex
var toDeviceTransactionBeforeInsertHooks []ToDeviceTransactionHook
var toDeviceTransactionAfterInsertMu sync.Mutex
var toDeviceTransactionAfterInsertHooks []ToDeviceTransactionHook

var toDeviceTransactionBeforeUpdateMu sync.Mutex
var toDeviceTransactionBeforeUpdateHooks []ToDeviceTransactionHook
var toDeviceTransactionAfterUpdateMu sync.Mutex
var toDeviceTransactionAfterUpdateHooks []ToDeviceTransactionHook

var toDeviceTransactionBeforeDeleteMu sync.Mutex
var toDeviceTransactionBeforeDeleteHooks []ToDeviceTransactionHook
var toDeviceTransactionAfterDeleteMu sync.Mutex
var toDeviceTransactionAfterDeleteHooks []ToDeviceTransactionHook

var toDeviceTransactionBeforeUpsertMu sync.Mutex
var toDeviceTransactionBeforeUpsertHooks []ToDeviceTransactionHook
var toDeviceTransactionAfterUpsertMu sync.Mutex
var toDeviceTransactionAfterUpsertHooks []ToDeviceTransactionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ToDeviceTransaction) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range toDeviceTransactionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ToDeviceTransaction) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range toDeviceTransactionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ToDeviceTransaction) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range toDeviceTransactionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ToDeviceTransaction) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range toDeviceTransactionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ToDeviceTransaction) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range toDeviceTransactionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ToDeviceTransaction) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range toDeviceTransactionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ToDeviceTransaction) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range toDeviceTransactionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ToDeviceTransaction) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range toDeviceTransactionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ToDeviceTransaction) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range toDeviceTransactionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddToDeviceTransactionHook registers your hook function for all future operations.
func AddToDeviceTransactionHook(hookPoint boil.HookPoint, toDeviceTransactionHook ToDeviceTransactionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		toDeviceTransactionAfterSelectMu.Lock()
		toDeviceTransactionAfterSelectHooks = append(toDeviceTransactionAfterSelectHooks, toDeviceTransactionHook)
		toDeviceTransactionAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		toDeviceTransactionBeforeInsertMu.Lock()
		toDeviceTransactionBeforeInsertHooks = append(toDeviceTransactionBeforeInsertHooks, toDeviceTransactionHook)
		toDeviceTransactionBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		toDeviceTransactionAfterInsertMu.Lock()
		toDeviceTransactionAfterInsertHooks = append(toDeviceTransactionAfterInsertHooks, toDeviceTransactionHook)
		toDeviceTransactionAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		toDeviceTransactionBeforeUpdateMu.Lock()
		toDeviceTransactionBeforeUpdateHooks = append(toDeviceTransactionBeforeUpdateHooks, toDeviceTransactionHook)
		toDeviceTransactionBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		toDeviceTransactionAfterUpdateMu.Lock()
		toDeviceTransactionAfterUpdateHooks = append(toDeviceTransactionAfterUpdateHooks, toDeviceTransactionHook)
		toDeviceTransactionAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		toDeviceTransactionBeforeDeleteMu.Lock()
		toDeviceTransactionBeforeDeleteHooks = append(toDeviceTransactionBeforeDeleteHooks, toDeviceTransactionHook)
		toDeviceTransactionBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		toDeviceTransactionAfterDeleteMu.Lock()
		toDeviceTransactionAfterDeleteHooks = append(toDeviceTransactionAfterDeleteHooks, toDeviceTransactionHook)
		toDeviceTransactionAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		toDeviceTransactionBeforeUpsertMu.Lock()
		toDeviceTransactionBeforeUpsertHooks = append(toDeviceTransactionBeforeUpsertHooks, toDeviceTransactionHook)
		toDeviceTransactionBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		toDeviceTransactionAfterUpsertMu.Lock()
		toDeviceTransactionAfterUpsertHooks = append(toDeviceTransactionAfterUpsertHooks, toDeviceTransactionHook)
		toDeviceTransactionAfterUpsertMu.Unlock()
	}
}

// One returns a single toDeviceTransaction record from the query.
func (q toDeviceTransactionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ToDeviceTransaction, error) {
	o := &ToDeviceTransaction{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for to_device_transactions")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ToDeviceTransaction records from the query.
func (q toDeviceTransactionQuery) All(ctx context.Context, exec boil.ContextExecutor) (ToDeviceTransactionSlice, error) {
	var o []*ToDeviceTransaction

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to ToDeviceTransaction slice")
	}

	if len(toDeviceTransactionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ToDeviceTransaction records in the query.
func (q toDeviceTransactionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count to_device_transactions rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q toDeviceTransactionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if to_device_transactions exists")
	}

	return count > 0, nil
}

// ToDeviceTransactions retrieves all the records using an executor.
func ToDeviceTransactions(mods ...qm.QueryMod) toDeviceTransactionQuery {
	mods = append(mods, qm.From("\"to_device_transactions\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"to_device_transactions\".*"})
	}

	return toDeviceTransactionQuery{q}
}

// FindToDeviceTransaction retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindToDeviceTransaction(ctx context.Context, exec boil.ContextExecutor, tenantID string, userID string, deviceID string, tXNID string, selectCols ...string) (*ToDeviceTransaction, error) {
	toDeviceTransactionObj := &ToDeviceTransaction{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"to_device_transactions\" where \"tenant_id\"=$1 AND \"user_id\"=$2 AND \"device_id\"=$3 AND \"txn_id\"=$4", sel,
	)

	q := queries.Raw(query, tenantID, userID, deviceID, tXNID)

	err := q.Bind(ctx, exec, toDeviceTransactionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from to_device_transactions")
	}

	if err = toDeviceTransactionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return toDeviceTransactionObj, err
	}

	return toDeviceTransactionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ToDeviceTransaction) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no to_device_transactions provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(toDeviceTransactionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	toDeviceTransactionInsertCacheMut.RLock()
	cache, cached := toDeviceTransactionInsertCache[key]
	toDeviceTransactionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			toDeviceTransactionAllColumns,
			toDeviceTransactionColumnsWithDefault,
			toDeviceTransactionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(toDeviceTransactionType, toDeviceTransactionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(toDeviceTransactionType, toDeviceTransactionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"to_device_transactions\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"to_device_transactions\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into to_device_transactions")
	}

	if !cached {
		toDeviceTransactionInsertCacheMut.Lock()
		toDeviceTransactionInsertCache[key] = cache
		toDeviceTransactionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ToDeviceTransaction.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ToDeviceTransaction) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	toDeviceTransactionUpdateCacheMut.RLock()
	cache, cached := toDeviceTransactionUpdateCache[key]
	toDeviceTransactionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			toDeviceTransactionAllColumns,
			toDeviceTransactionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update to_device_transactions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"to_device_transactions\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, toDeviceTransactionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(toDeviceTransactionType, toDeviceTransactionMapping, append(wl, toDeviceTransactionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update to_device_transactions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for to_device_transactions")
	}

	if !cached {
		toDeviceTransactionUpdateCacheMut.Lock()
		toDeviceTransactionUpdateCache[key] = cache
		toDeviceTransactionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q toDeviceTransactionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for to_device_transactions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for to_device_transactions")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ToDeviceTransactionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), toDeviceTransactionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"to_device_transactions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, toDeviceTransactionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in toDeviceTransaction slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all toDeviceTransaction")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ToDeviceTransaction) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no to_device_transactions provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(toDeviceTransactionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	toDeviceTransactionUpsertCacheMut.RLock()
	cache, cached := toDeviceTransactionUpsertCache[key]
	toDeviceTransactionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			toDeviceTransactionAllColumns,
			toDeviceTransactionColumnsWithDefault,
			toDeviceTransactionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			toDeviceTransactionAllColumns,
			toDeviceTransactionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert to_device_transactions, could not build update column list")
		}

		ret := strmangle.SetComplement(toDeviceTransactionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(toDeviceTransactionPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert to_device_transactions, could not build conflict column list")
			}

			conflict = make([]string, len(toDeviceTransactionPrimaryKeyColumns))
			copy(conflict, toDeviceTransactionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"to_device_transactions\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(toDeviceTransactionType, toDeviceTransactionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(toDeviceTransactionType, toDeviceTransactionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert to_device_transactions")
	}

	if !cached {
		toDeviceTransactionUpsertCacheMut.Lock()
		toDeviceTransactionUpsertCache[key] = cache
		toDeviceTransactionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ToDeviceTransaction record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ToDeviceTransaction) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no ToDeviceTransaction provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), toDeviceTransactionPrimaryKeyMapping)
	sql := "DELETE FROM \"to_device_transactions\" WHERE \"tenant_id\"=$1 AND \"user_id\"=$2 AND \"device_id\"=$3 AND \"txn_id\"=$4"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from to_device_transactions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for to_device_transactions")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q toDeviceTransactionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no toDeviceTransactionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from to_device_transactions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for to_device_transactions")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ToDeviceTransactionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(toDeviceTransactionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), toDeviceTransactionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"to_device_transactions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, toDeviceTransactionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from toDeviceTransaction slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for to_device_transactions")
	}

	if len(toDeviceTransactionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ToDeviceTransaction) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindToDeviceTransaction(ctx, exec, o.TenantID, o.UserID, o.DeviceID, o.TXNID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ToDeviceTransactionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ToDeviceTransactionSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), toDeviceTransactionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"to_device_transactions\".* FROM \"to_device_transactions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, toDeviceTransactionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in ToDeviceTransactionSlice")
	}

	*o = slice

	return nil
}

// ToDeviceTransactionExists checks if the ToDeviceTransaction row exists.
func ToDeviceTransactionExists(ctx context.Context, exec boil.ContextExecutor, tenantID string, userID string, deviceID string, tXNID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"to_device_transactions\" where \"tenant_id\"=$1 AND \"user_id\"=$2 AND \"device_id\"=$3 AND \"txn_id\"=$4 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, tenantID, userID, deviceID, tXNID)
	}
	row := exec.QueryRowContext(ctx, sql, tenantID, userID, deviceID, tXNID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if to_device_transactions exists")
	}

	return exists, nil
}

// Exists checks if the ToDeviceTransaction row exists.
func (o *ToDeviceTransaction) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ToDeviceTransactionExists(ctx, exec, o.TenantID, o.UserID, o.DeviceID, o.TXNID)
}
